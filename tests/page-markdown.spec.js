var chai = require('chai');
var build = function (content) {
  //
};

describe('markdown explanations', function() {

  var result = build('./samples/markdown-sample.md');
  var resultWithCode = build('./samples/markdown-codeblock-sample.md');

});
